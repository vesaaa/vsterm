# VsTerm 实现状态

## 已完成

| 项 | 说明 |
|----|------|
| CJK 字体 | 加载系统 `simhei.ttf` / 雅黑，修复菜单乱码 |
| i18n | 简体中文 / English，菜单「语言」可切换，写入 `~/.vsterm/config.yaml` |
| 左栏双 Tab | **服务器** / **系统监控**；打开终端后自动切到监控 |
| 系统监控 | CPU / 内存 / Swap 条、进程表（CPU 降序）、网卡流量图（可切换接口） |
| 主区工具栏 | 系统信息 / 文件传输 / 常用命令 快捷入口 |
| 系统信息窗 | 主机名、OS、内核、架构、CPU、内存、网卡、磁盘 |
| 底部双 Tab | **文件**（本地预览 + SFTP 骨架）/ **命令**（`commands.yaml`，点击写入终端） |

## 进行中 / 下一步

1. 远端 SSH 真正建连（russh + 系统 ssh），监控数据改从远端 `/proc` 采集  
2. SFTP 文件上传下载落地  
3. 凭据 vault 接入 UI 新建会话对话框  
4. 主题与快捷键打磨  
