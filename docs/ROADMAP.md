# VsTerm 实现状态

## 已完成

| 项 | 说明 |
|----|------|
| CJK 字体 | 加载系统 `simhei.ttf` / 雅黑，修复菜单乱码 |
| i18n | 简体中文 / English，菜单「语言」可切换，写入 `~/.vsterm/config.yaml` |
| 左栏双 Tab | **服务器** / **系统监控**；打开终端后自动切到监控 |
| 系统监控 | CPU / 内存 / Swap 条、进程表（CPU 降序）、网卡流量图（可切换接口） |
| 主区工具栏 | 系统信息 / 文件传输 / 常用命令 快捷入口 |
| 系统信息窗 | 主机名、OS、内核、架构、CPU、内存、网卡、磁盘 |
| 底部双 Tab | **文件**（本地 + russh SFTP 列表/单文件上下传）/ **命令**（`commands.yaml`，点击写入终端） |

## 进行中 / 下一步

1. ~~远端 SSH 真正建连~~ ✅ 内置 russh（双击会话树、vault 凭据、友好错误提示）
2. ~~远端监控（按连接采集 `/proc`）~~ ✅ 进程 / 网卡 / 存储 / 路由绑定远端主机  
3. ~~添加 / 管理服务器 UI~~ ✅ 新建编辑删除会话、文件夹与 vault 密码录入  
4. ~~russh 内置引擎~~ ✅ 终端、远程命令与 SFTP 复用同一认证会话  
5. ~~SFTP 文件上传下载落地~~ ✅ russh 真 SFTP（列表/导航/文件及目录上下传/进度）  
6. 主题与快捷键打磨  

